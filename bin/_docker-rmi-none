#!/usr/bin/env bash

imagesList="$(docker images | grep -E "^<none>" | awk 'BEGIN {FS=" "} {print $3}' | tr "\n" " ")"

if [ -z "$imagesList" ]; then
  echo "No <none> images to remove"
  exit -1
fi

for i in ${imagesList[@]}; do
  docker rmi "$i"
done
exit 0
